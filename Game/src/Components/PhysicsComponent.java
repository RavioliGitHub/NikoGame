package Components;

/**
 * For everything that is physically on the map
 */
public class PhysicsComponent extends Component{
    public PhysicsComponent(int ID, Class componentSubClass) {
        super(ID, componentSubClass);
    }
}